name = "stemlearn-mcp"
main = "src/worker.ts"
compatibility_date = "2025-11-01"
compatibility_flags = ["nodejs_compat"]

# This Worker entry point is retained for development only; local stdio is the
# supported deployment model. Do not deploy a student's Moodle token remotely.
